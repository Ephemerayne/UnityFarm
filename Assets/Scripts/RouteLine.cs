using UnityEngine;

public class RouteLine: MonoBehaviour
{
/*    [SerializeField] private Transform[] points;
    [SerializeField] private RouteLineController controller;

    private void Start()
    {
        controller.SetUpLine(points);
    }*/
}
